<h1><%= @task.title %></h1>
<button><%= link_to "Edit task", edit_path %></button>
<p><%= @task.details %></p>
<%= @task.completed == true ? '<i class="fas fa-check"></i>'.html_safe : '<i class="fal fa-square"></i>'.html_safe %>
<%= @task.completed == true ? "this task is completed" : "This task is not completed yet" %>
<p> <%= link_to "back to list", tasks_path %> </p>
